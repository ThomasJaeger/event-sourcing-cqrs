<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>EventSourcingCqrs.Hosts.Web</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Application\Application.csproj" />
    <ProjectReference Include="..\..\Domain\Domain.csproj" />
    <ProjectReference Include="..\..\Infrastructure\SignalR\SignalR.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Hosts.Web.Tests" />
    <!-- IntegrationTests resolves the internal in-process notification services through a
         WebApplicationFactory to verify the host's DI composition (dispatcher singleton, subscription
         scoped). ADR 0032. -->
    <InternalsVisibleTo Include="IntegrationTests" />
  </ItemGroup>

</Project>
