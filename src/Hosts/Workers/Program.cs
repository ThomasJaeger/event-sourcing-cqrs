// Placeholder. Commit 8 supplies the real composition root: env-var
// reading, migrations orchestration, host.RunAsync against the
// WorkersHostFactory.
Console.WriteLine("Workers host placeholder.");
return 0;
